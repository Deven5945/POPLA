# POPLA Focus Desk

플래너와 포모도로를 결합한 Flet 데스크톱 앱입니다

테스트를 Mac에서만 해본지라 다른 OS에서 정상적으로 작동하지 않을 수 있스빈다

작업 목록은 `saves/plan.json`에 저장됩니다

## 실행

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python3 main.py
```

